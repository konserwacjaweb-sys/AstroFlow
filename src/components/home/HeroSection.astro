---
import { Image } from 'astro:assets';
import heroImage from '../../assets/bg-4.jpg';
import circleGoldBg from '../../assets/circlegoldbg.png';

export interface Props {
  heading?: string;
  subheading?: string;
  circleHeading?: string;
  circleSubheading?: string;
}

const defaultHeading = "Piękno w Każdym Detalu";

const {
  heading = defaultHeading,
  subheading = "Profesjonalne usługi makijażu permanentnego & powiększania ust",
  circleHeading = "Naturalny Efekt",
  circleSubheading = "Podkreśl swoje piękno subtelnie i elegancko",
} = Astro.props;

const useDefaultHeading = heading === defaultHeading;
---

<section class="relative w-auto h-[70vh] md:h-screen lg:h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Image -->
  <Image
    src={heroImage}
    alt="Makijaz permanentny"
    class="absolute inset-0 w-full h-full object-cover object-top"
    loading="eager"
    quality="high"
  />

  <!-- Content -->
  <div class="w-full px-8 relative z-10 pt-28 sm:pt-44 lg:pt-52 pb-12 sm:pb-16">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 sm:gap-10 lg:gap-16 items-center ">
      <div class="text-left">
        <h1 class="leading-tight text-white mb-4 sm:mb-6 drop-shadow-sm text-5xl md:text-5xl lg:text-7xl">
          {useDefaultHeading ? (
            <>
              Piękno <span class="absolute left-[35%] top-[12%] md:left-[42%] md:top-[15%] lg:left-[53%] lg:top-[15%] -translate-x-1/2 -translate-y-1/2 text-[150px] md:text-[250px] lg:text-[376px] opacity-35 leading-none pointer-events-none italic text-white/60">w</span> <br />Każdym Detalu
            </>
          ) : (
            heading
          )}
        </h1>
        <p class="text-sm md:text-base tracking-[0.16em] uppercase font-light text-white/90 drop-shadow-md max-w-xl mx-0">
          {subheading}
        </p>
      </div>

      <div class="relative w-full h-56 sm:h-72 md:h-80 hidden md:flex items-center justify-end">
        <div class="absolute right-0 sm:right-0 md:right-0 lg:right-0 top-[55%] sm:top-1/2 -translate-y-1/2 h-32 w-32 sm:h-48 sm:w-48 md:h-64 md:w-64 lg:h-80 lg:w-80">
          <Image
            src={circleGoldBg}
            alt="Złote koło dekoracyjne"
            class="w-full h-full object-contain"
            loading="eager"
            quality="high"
          />
        </div>

        <div class="flex flex-col items-end text-right">
          <h4 class="font-notoDisplay text-white text-lg sm:text-2xl lg:text-3xl font-normal leading-tight mb-2 sm:mb-3 max-w-70">
            {circleHeading}
          </h4>
          <p class="text-white/90 text-sm sm:text-base leading-relaxed max-w-65">
            {circleSubheading}
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
